Finance keeps a large FY24 audit markdown at /app/data/audit_archive.md. The Node project at /app/audit-extractor should run node /app/audit-extractor/src/cli.js extract --input <path> --outdir <dir> and write four reports, but several modules are wrong. Fix the source under /app/audit-extractor (do not hand-copy files into /app/output). Graders always run extract with --input /app/data/audit_archive.md and --outdir /app/output and compare those outputs to the archive policy.

All row logic lives in the archive, not here. Read ## Reconciliation and Exception Policy (FY24 Audit Handbook) at the top, then the rest of the file (## Email Excerpts, ## Policy Exceptions, ## Correction Notices, and the real ## Transaction Ledger near the end). Implement that handbook in src/parser/* and src/normalize/reconcile.js: canonical ledger from a line that is exactly ## Transaction Ledger (skip ## Transaction Ledger (working copy) and provisional: true rows; when an id repeats, keep the last non-provisional block in file order), then signed #### Amendment for TXN-... owner changes, then compliance@ status changes only when sent: is on or after the ledger date and status moves up the handbook precedence ladder, then correction notices last with the greatest effective date winning per transaction and field. Parse sections with regex on whole heading lines, not indexOf, because the handbook mentions those titles in prose.

Graded file shape: transactions.json must be {"items":[...]} (not {"transactions":...}), sorted by transaction_id, fields transaction_id, owner, lowercase status, effective_date (YYYY-MM-DD), amount_usd (JSON number, two decimals), exception_reason (null or semicolon-joined codes from the handbook). transactions.csv header must be transaction_id,owner,status,effective_date,amount_usd,exception_reason with the same rows and order; leave exception_reason empty when null. exceptions.json must be {"items":[...]} (not {"exceptions":...}) containing only rows with exception_reason set. reconciliation_report.jsonl is one JSON object per winning correction notice (notice_id, transaction_id, field, previous_value, new_value, effective), sorted by notice_id; previous_value is the value immediately before that notice applies. Missing --input or --outdir prints to stderr and exits 1.
