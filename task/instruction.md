Finance QA says the FY24 audit extractor at `/app/audit-extractor` is reconciling `/app/data/audit_archive.md` wrong — statuses drift between reruns, compliance holds miss later mail, and correction notices land on the wrong ledger rows. The input is one large FY24 audit handbook at `/app/data/audit_archive.md` (investigation briefs, committee minutes, mail excerpts, policy exceptions, correction notices, and ledger rows).

Binding reconciliation policy is distributed across Investigation Briefs 01–09, including a mid-year amendment in Brief 09 that overrides earlier hold wording; you need to read the full archive and reconcile rules across those sections and the structured data near the file end. Draft threads, case-study filler, and decoy section headings are not authoritative.

Fix the Node sources and run `node /app/audit-extractor/src/cli.js extract --input <path> --outdir <dir>`; do not hand-copy files into `/app/output`. Graders always pass `--input /app/data/audit_archive.md` and `--outdir /app/output`. Missing `--input` or `--outdir` should print to stderr and exit 1. Write `transactions.json`, `transactions.csv`, `exceptions.json`, and `reconciliation_report.jsonl` under `--outdir`; `/app/audit-extractor/docs/extractor-output-contract.md` defines row shapes. Leave `/tests/` alone.
