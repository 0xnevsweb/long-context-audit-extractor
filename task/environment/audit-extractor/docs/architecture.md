# Audit Extractor

Node CLI under src/ with subcommand extract. Modules under src/parser read the markdown input; src/normalize applies archive policy; src/output writes JSON, CSV, and JSONL to a caller-provided directory.
